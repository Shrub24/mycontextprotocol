pre-commit:
  commands:
    helm-lint:
      glob: "infra/k8s/charts/**/*.yaml"
      run: |
        cd infra/k8s && helm lint charts/*
    
    yamllint:
      glob: "**/*.{yaml,yml}"
      exclude: "infra/k8s/charts/**/*.yaml"
      run: yamllint {staged_files}
