---
phase: 01-tooling-hygiene
plan: 01
type: execute
---

<objective>
Finalize pre-commit formatting and linting flow with deterministic ordering and auto-restaging.

Purpose: Ensure every commit enforces format + lint gates with reliable, modern tooling behavior.
Output: Updated `lefthook.yml` and verified pre-commit pipeline.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@lefthook.yml
@.yamlfmt.yaml
@.yamllint.yaml
@.editorconfig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize lefthook pre-commit pipeline</name>
  <files>lefthook.yml</files>
  <action>Update `pre-commit` to be the single enforcement point: set `stage_fixed: true`, keep `parallel: true`, and define one command per domain to avoid parallel race conditions. In the python command, enforce order: `ruff check --fix` → `ruff format` → `ruff check` → `basedpyright src tests`. In the YAML command, run `yamlfmt -conf .yamlfmt.yaml` then `yamllint`. Keep kube-linter and helm lint as separate commands. Remove any `|| true` or `fail: false` gates so linting blocks commits. Remove the pre-push hook entirely.</action>
  <verify>Run `lefthook run pre-commit --force` and confirm it exits 0.</verify>
  <done>lefthook config matches required ordering, blocking behavior, and auto-restaging via `stage_fixed: true`.</done>
</task>

<task type="auto">
  <name>Task 2: Validate auto-restaging behavior</name>
  <files>lefthook.yml</files>
  <action>After running `lefthook run pre-commit --force`, ensure any formatter changes are auto-staged and no unstaged diffs remain. Use `git status` to confirm clean or only staged changes.</action>
  <verify>`git status` shows no unstaged changes after the hook completes.</verify>
  <done>Pre-commit formatters re-stage changes automatically and leave the working tree clean.</done>
</task>

</tasks>

<verification>
- `lefthook run pre-commit --force` succeeds
- `git status` shows no unstaged changes after hooks run
</verification>

<success_criteria>
- `lefthook.yml` enforces ordered formatting + linting with `stage_fixed: true`
- Python, YAML, kube-linter, and helm-lint checks run without non-blocking gates
- Pre-push hook removed
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene/01-01-SUMMARY.md`:

# Phase 1 Plan 1: Tooling hygiene Summary

**[One-line summary of the pre-commit pipeline update]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
