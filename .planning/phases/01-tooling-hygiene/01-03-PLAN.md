---
phase: 01-tooling-hygiene
plan: 03
type: execute
---

<objective>
Align Taskfile-based tooling with the finalized pre-commit pipeline and update dev docs.

Purpose: Ensure task commands and documentation reflect the enforced hook behavior.
Output: Updated taskfiles and DEVELOPMENT.md.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@Taskfile.yml
@.taskfiles/format.yml
@.taskfiles/lint.yml
@.taskfiles/typecheck.yml
@.taskfiles/test.yml
@DEVELOPMENT.md
@lefthook.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align Taskfile commands with hook behavior</name>
  <files>Taskfile.yml, .taskfiles/format.yml, .taskfiles/lint.yml, .taskfiles/typecheck.yml</files>
  <action>Review ensure Taskfile tasks mirror the enforced hook behavior: `task format` should run `ruff check --fix` then `ruff format` and `yamlfmt -conf .yamlfmt.yaml`; `task lint` should run `ruff check` (no fix), `basedpyright`, `yamllint`, `kube-linter`, `helm lint`, and `kubeconform` in a deterministic order. Keep task naming as-is (lint = full suite). Update tasks only where they diverge from the hook pipeline.</action>
  <verify>`task format` completes without errors and leaves no diffs; `task lint` completes without errors.</verify>
  <done>Taskfile tooling commands match hook ordering and coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh DEVELOPMENT.md to reflect final workflow</name>
  <files>DEVELOPMENT.md</files>
  <action>Update the developer guide to describe the finalized pre-commit flow (ruff fix → format → check → basedpyright, yamlfmt + yamllint, kube-linter, helm-lint), and clarify that `task lint` is the full suite while `task format` is the auto-fix formatter. Keep it concise and aligned with current task names.</action>
  <verify>`DEVELOPMENT.md` accurately reflects the hook and Taskfile behavior.</verify>
  <done>Docs reflect the real tooling pipeline and task intent.</done>
</task>

</tasks>

<verification>
- `task format` and `task lint` succeed
- DEVELOPMENT.md matches hook + task semantics
</verification>

<success_criteria>
- Taskfile tooling commands mirror hook behavior
- Developer docs reflect final pre-commit pipeline and task intent
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene/01-03-SUMMARY.md`:

# Phase 1 Plan 3: Tooling hygiene Summary

**[One-line summary of taskfile/doc alignment]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
