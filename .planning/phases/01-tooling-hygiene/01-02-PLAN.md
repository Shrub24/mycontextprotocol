---
phase: 01-tooling-hygiene
plan: 02
type: execute
---

<objective>
Align formatter and lint configurations to eliminate editor/CLI diffs (especially YAML).

Purpose: Ensure formatting is deterministic across editors, CLI, and hooks.
Output: Harmonized formatting configs and YAML baseline.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.editorconfig
@.yamlfmt.yaml
@.yamllint.yaml
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize formatting configuration</name>
  <files>.editorconfig, .yamlfmt.yaml, .yamllint.yaml, pyproject.toml</files>
  <action>Review and align formatting rules for YAML and Python: ensure YAML indent=2, line length=100, LF line endings, and trailing whitespace handling consistent across `.editorconfig`, `yamlfmt` config, and `yamllint`. Ensure `ruff` formatting line length matches (if configured) and document any intentional exceptions (e.g., Markdown trailing whitespace). Update configs to remove mismatches that cause blank-line diffs in editors.</action>
  <verify>`yamlfmt -conf .yamlfmt.yaml -lint infra/k8s` and `yamllint .` report no formatting violations.</verify>
  <done>Formatter configs agree on YAML rules and editor diffs no longer appear for yamlfmt runs.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize YAML formatting baseline</name>
  <files>infra/k8s/**/*.yaml, **/*.yml</files>
  <action>Run `yamlfmt -conf .yamlfmt.yaml` against the repository to normalize YAML formatting. Re-run `yamllint` to confirm lint passes. Keep helm chart templates excluded per existing configs.</action>
  <verify>`yamlfmt -conf .yamlfmt.yaml -lint .` and `yamllint .` succeed.</verify>
  <done>YAML files are formatted consistently with no blank-line churn in editors or hooks.</done>
</task>

</tasks>

<verification>
- `yamlfmt -conf .yamlfmt.yaml -lint .` passes
- `yamllint .` passes
- `ruff format --check src tests` passes
</verification>

<success_criteria>
- YAML formatting is deterministic across editor + CLI
- Config mismatch causes are eliminated
- No blank-line diffs after running yamlfmt
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-hygiene/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Tooling hygiene Summary

**[One-line summary of formatter alignment and YAML normalization]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
