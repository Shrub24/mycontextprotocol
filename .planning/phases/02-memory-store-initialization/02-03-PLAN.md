---
phase: 02-memory-store-initialization
plan: 03
type: execute
---

<objective>
Stabilize LightRAG deployment inputs and secrets while targeting the multi-DB Postgres layout.

Purpose: Make LightRAG deployment reproducible and ensure it can self-create its Postgres tables using AGE + pgvector.
Output: Pinned LightRAG chart source, GHCR image configuration, and SOPS-managed secrets.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-store-initialization/DISCOVERY.md
@.planning/phases/02-memory-store-initialization/02-RESEARCH.md
@infra/k8s/helmfile.yaml
@infra/k8s/values/lightrag/defaults.yaml
@infra/k8s/values/lightrag/local.yaml
@infra/k8s/values/lightrag/production.yaml
@infra/k8s/.sops.yaml
@infra/k8s/secrets/common.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin LightRAG Helm chart source</name>
  <files>infra/k8s/helmfile.yaml, infra/k8s/charts/lightrag/**</files>
  <action>Vendor the LightRAG Helm chart into the repo (e.g., `infra/k8s/charts/lightrag`) from the LightRAG GitHub repo at a pinned commit/tag. Update `infra/k8s/helmfile.yaml` to reference the in-repo chart path instead of `/tmp/...`. Keep the existing release name and namespace. Document the pinned commit in the chart directory (README or comment). Avoid relying on `/tmp` paths so Helmfile is reproducible.</action>
  <verify>`helmfile -l name=lightrag template` uses the in-repo chart path with no missing path errors.</verify>
  <done>LightRAG chart source is pinned and reproducible in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Configure LightRAG image + SOPS secrets</name>
  <files>infra/k8s/values/lightrag/defaults.yaml, infra/k8s/values/lightrag/production.yaml, infra/k8s/secrets/lightrag/production.yaml, infra/k8s/secrets/lightrag/local.yaml</files>
  <action>Update LightRAG values to use the GHCR image `ghcr.io/hkuds/lightrag` with an explicit tag (choose a stable release tag; document it). Create SOPS-encrypted secrets for LightRAG (`lightrag-secrets`) with `POSTGRES_PASSWORD` and `LIGHTRAG_API_KEY` for both local and production. Use a dev placeholder key for local if needed, but keep the key present in all environments for consistent auth behavior. Ensure values reference the `lightrag-secrets` secret via envFrom without hardcoding secrets in values files.</action>
  <verify>`helmfile -l name=lightrag template` shows the GHCR image + envFrom secret and no plaintext secrets in values.</verify>
  <done>LightRAG runs from GHCR image with SOPS-managed secrets and API key required in all environments.</done>
</task>

<task type="auto">
  <name>Task 3: Verify LightRAG Postgres wiring for multi-DB</name>
  <files>infra/k8s/values/lightrag/defaults.yaml</files>
  <action>Confirm LightRAG values target the `lightrag` database and use the shared `app` role. Do not add AGE requirements to other DBs. Ensure config declares graph/vector/kv storage as Postgres-backed and relies on self-creation of tables on startup.</action>
  <verify>Rendered LightRAG manifest uses `POSTGRES_DATABASE=lightrag` and `POSTGRES_USER=app`.</verify>
  <done>LightRAG is configured to self-initialize its Postgres tables in the correct DB.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `helmfile -l name=lightrag template` succeeds with in-repo chart path
- [ ] LightRAG render shows GHCR image + envFrom secret
- [ ] LightRAG config targets `lightrag` DB
</verification>

<success_criteria>

- LightRAG chart source is pinned in-repo
- LightRAG uses GHCR image and SOPS-managed secrets
- LightRAG config is aligned to multi-DB layout
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Memory store initialization Summary

**[One-line summary of LightRAG chart pinning + secrets + GHCR image]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
