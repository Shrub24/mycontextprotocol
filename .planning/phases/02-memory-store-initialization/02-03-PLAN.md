---
phase: 02-memory-store-initialization
plan: 03
type: execute
---

<objective>
Stabilize LightRAG deployment inputs and secrets while targeting the multi-DB Postgres layout.

Purpose: Make LightRAG deployment reproducible and ensure it can self-create its Postgres tables using AGE + pgvector.
Output: Pinned LightRAG chart source, GHCR image configuration, and SOPS-managed secrets.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-store-initialization/DISCOVERY.md
@.planning/phases/02-memory-store-initialization/02-RESEARCH.md
@infra/k8s/helmfile.yaml
@infra/k8s/values/lightrag/defaults.yaml
@infra/k8s/values/lightrag/local.yaml
@infra/k8s/values/lightrag/production.yaml
@infra/k8s/secrets/templates/
@infra/k8s/secrets/profiles/
@.sops.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin LightRAG Helm chart source</name>
  <files>infra/k8s/helmfile.yaml, infra/k8s/charts/lightrag/**</files>
  <action>Create a thin in-repo Helm chart at `infra/k8s/charts/lightrag` that deploys the LightRAG OCI image. Update `infra/k8s/helmfile.yaml` to reference the in-repo chart path instead of `/tmp/...`. Keep the existing release name and move the release to the shared `mycontextprotocol` namespace to match the single-namespace stack. Do not pin the image tag; use the latest image unless explicitly directed. Remove or archive `infra/k8s/manifests/lightrag.yaml` so it is not used alongside Helm.</action>
  <verify>`helmfile -l name=lightrag template` uses the in-repo chart path with no missing path errors.</verify>
  <done>LightRAG chart source is pinned and reproducible in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Configure LightRAG image + SOPS external secrets</name>
  <files>infra/k8s/values/lightrag/defaults.yaml, infra/k8s/values/lightrag/production.yaml, infra/k8s/secrets/profiles/prod/lightrag-secrets.yaml, infra/k8s/secrets/templates/lightrag-secrets.yaml</files>
  <action>Update LightRAG values to use the GHCR image `ghcr.io/hkuds/lightrag` without pinning a tag. Create a SOPS-encrypted Secret manifest `lightrag-secrets` in the `mycontextprotocol` namespace under `infra/k8s/secrets/profiles/prod/`, containing only external API keys (`LIGHTRAG_API_KEY`, `LLM_BINDING_API_KEY`, `EMBEDDING_BINDING_API_KEY`). Add a plaintext template under `infra/k8s/secrets/templates/` with TODO markers. Ensure values reference the `lightrag-secrets` secret via envFrom while Postgres credentials are sourced from the CNPG `<cluster>-app` secret.</action>
  <verify>`helmfile -l name=lightrag template` shows the GHCR image, envFrom `lightrag-secrets`, and no plaintext secrets in values.</verify>
  <done>LightRAG runs from GHCR image with SOPS-managed external secrets, while Postgres credentials come from CNPG.</done>
</task>

<task type="auto">
  <name>Task 3: Verify LightRAG Postgres wiring for multi-DB</name>
  <files>infra/k8s/values/lightrag/defaults.yaml</files>
  <action>Confirm LightRAG values target the `lightrag` database and use the shared `app` role. Wire `POSTGRES_PASSWORD` from the CNPG `<cluster>-app` secret and avoid SOPS-managed internal DB credentials. Ensure config declares graph/vector/kv storage as Postgres-backed and relies on self-creation of tables on startup.</action>
  <verify>Rendered LightRAG manifest uses `POSTGRES_DATABASE=lightrag`, `POSTGRES_USER=app`, and `POSTGRES_PASSWORD` sourced from the CNPG app secret.</verify>
  <done>LightRAG is configured to self-initialize its Postgres tables with CNPG-managed credentials in the correct DB.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `helmfile -l name=lightrag template` succeeds with in-repo chart path
- [ ] LightRAG render shows GHCR image + envFrom secret
- [ ] LightRAG config targets `lightrag` DB
</verification>

<success_criteria>

- LightRAG chart source is pinned in-repo
- LightRAG uses GHCR image and SOPS-managed secrets
- LightRAG config is aligned to multi-DB layout
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Memory store initialization Summary

**[One-line summary of LightRAG chart pinning + secrets + GHCR image]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
