---
phase: 02-memory-store-initialization
plan: 04
type: plan
---

<objective>
Standardize secret provisioning across charts with a single, reusable interface and decouple internal secret ownership from workloads.

Purpose: Make external secrets provider-agnostic (SOPS, ESO, manual) and keep internal secrets owned by the stack, not individual services.
Output: Library chart + unified values contract, refactored gateway/worker/lightrag charts, and a dedicated dragonfly secrets release.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/plan-phase.md
./summary.md
</execution_context>

<context>
@infra/k8s/helmfile.yaml
@infra/k8s/values/common.yaml
@infra/k8s/charts/gateway/values.yaml
@infra/k8s/charts/worker/values.yaml
@infra/k8s/charts/lightrag/values.yaml
@infra/k8s/charts/gateway/templates/**
@infra/k8s/charts/worker/templates/**
@infra/k8s/charts/lightrag/templates/**
@infra/k8s/secrets/**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Helm library chart</name>
  <files>infra/k8s/charts/mcp-lib/**</files>
  <action>Create a Helm library chart (`type: library`) with shared helpers for external secret wiring and consistent naming. Provide helpers for: external secret name resolution, envFrom rendering, and optional secret creation when `externalSecrets.create=true`. Keep it minimal (helpers only, no resources).</action>
  <verify>Charts can `include` helper functions without rendering library resources.</verify>
  <done>Library chart exists and is referenced by workloads.</done>
</task>

<task type="auto">
  <name>Task 2: Unify external secrets interface across workloads</name>
  <files>infra/k8s/charts/gateway/**, infra/k8s/charts/worker/**, infra/k8s/charts/lightrag/**</files>
  <action>Refactor gateway/worker/lightrag charts to use the same `externalSecrets` interface. Default to a shared secret name (`mycontextprotocol-external`) unless an explicit `externalSecrets.existingSecretName` is provided. Support both modes: (1) existing secret reference, (2) create secret from values (for helm-secrets). Remove per-chart secret fields and legacy hostname/namespace references.</action>
  <verify>Helmfile renders show all workloads mounting the same external secret name (or existing override), with no duplicate secret fields in chart values.</verify>
  <done>External secrets are provider-agnostic and consistent across charts.</done>
</task>

<task type="auto">
  <name>Task 3: Move Dragonfly password to stack-owned release</name>
  <files>infra/k8s/charts/dragonfly-secrets/**, infra/k8s/helmfile.yaml, infra/k8s/charts/gateway/templates/secret.yaml</files>
  <action>Create a dedicated `dragonfly-secrets` Helm release that generates the `dragonfly-password` Secret using `lookup` + `randAlphaNum`. Update Helmfile dependencies so dragonfly, gateway, and worker all depend on `dragonfly-secrets`. Remove dragonfly password generation from the gateway chart.</action>
  <verify>Rendered manifests show `dragonfly-password` created by the secrets release, and gateway/worker only reference it.</verify>
  <done>Dragonfly password is decoupled from gateway and owned by the stack.</done>
</task>

<task type="auto">
  <name>Task 4: Normalize secrets layout and clean legacy files</name>
  <files>infra/k8s/secrets/**</files>
  <action>Remove unused or legacy secret fields (e.g., old Dragonfly passwords) and align SOPS-managed files with the unified external secret interface. Ensure local/prod secrets stubs exist only where referenced by Helmfile and contain no plaintext placeholders outside templates.</action>
  <verify>Helmfile renders succeed without missing secrets or unused keys.</verify>
  <done>Secrets files are minimal, consistent, and aligned with the new interface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `helmfile -e local template` succeeds for gateway/worker/lightrag
- [ ] External secret name is consistent across workloads or overridden via `externalSecrets.existingSecretName`
- [ ] `dragonfly-password` is created by the stack-owned release
</verification>

<success_criteria>

- Workload charts share a single external secrets interface
- Dragonfly password is no longer owned by gateway
- Secrets files are consolidated and provider-agnostic
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-04-SUMMARY.md`:

# Phase 2 Plan 4: Memory store initialization Summary

**[One-line summary of shared secrets interface + dragonfly secret ownership]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
