---
phase: 02-memory-store-initialization
plan: 02
type: execute
---

<objective>
Ensure Alembic manages only app-owned tables and the app DB target is explicit.

Purpose: Prevent Alembic from touching Mem0/LlamaIndex/LightRAG tables and define the dedicated app database for internal tables.
Output: Corrected Alembic migration + app DB configuration.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-store-initialization/DISCOVERY.md
@.planning/phases/02-memory-store-initialization/02-RESEARCH.md
@infra/k8s/values/common.yaml
@infra/k8s/charts/gateway/templates/configmap.yaml
@src/mycontextprotocol/config.py
@alembic/env.py
@alembic/versions/84b435351e1f_initial_schema_inbox_table_only_.py
@src/mycontextprotocol/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Alembic revision to only manage app tables</name>
  <files>alembic/versions/84b435351e1f_initial_schema_inbox_table_only_.py</files>
  <action>Edit the initial Alembic revision so `upgrade()` only creates or alters app-owned tables (Inbox + related indexes) and does NOT drop or alter Mem0/LlamaIndex/LightRAG tables. Remove any DDL that drops or recreates mem0/llamaindex tables. Keep the migration focused on the SQLAlchemy models in `src/mycontextprotocol/models.py` only. Do not add new tables beyond the current models.</action>
  <verify>Review the migration file to confirm no references to mem0/llamaindex/lightrag tables exist; `uv run alembic upgrade head` succeeds against an empty `mycontextprotocol` database.</verify>
  <done>Alembic migration only manages app tables and is safe to run alongside self-managed stores.</done>
</task>

<task type="auto">
  <name>Task 2: Make app database explicit in settings and Helm values</name>
  <files>src/mycontextprotocol/config.py, infra/k8s/values/common.yaml, infra/k8s/charts/gateway/templates/configmap.yaml</files>
  <action>Add `postgres_database` (default `mycontextprotocol`) to settings and build the connection string using it instead of hardcoding `/postgres`. Wire `POSTGRES_DATABASE` into the gateway ConfigMap, sourced from `values/common.yaml` under a new `config.app.postgresDatabase` (or similar) entry. Avoid changing mem0/llamaindex DB settings in this task.</action>
  <verify>Gateway ConfigMap renders `POSTGRES_DATABASE` and the settings class produces `.../mycontextprotocol` by default.</verify>
  <done>App-owned tables target the `mycontextprotocol` database explicitly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run alembic upgrade head` succeeds against the app database
- [ ] Config renders `POSTGRES_DATABASE` for the gateway
</verification>

<success_criteria>

- Alembic no longer touches Mem0/LlamaIndex/LightRAG tables
- App DB target is explicit and configurable
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-02-SUMMARY.md`:

# Phase 2 Plan 2: Memory store initialization Summary

**[One-line summary of Alembic cleanup + app DB configuration]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
