---
phase: 02-memory-store-initialization
plan: 01
type: execute
---

<objective>
Prepare CNPG for multi-database memory stores with correct extensions and a single shared application role.

Purpose: Ensure mem0, llamaindex, lightrag, and app-owned tables can bootstrap cleanly in Postgres without manual DB provisioning.
Output: CNPG bootstrap SQL and secret wiring aligned with multi-DB layout + shared role.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-store-initialization/DISCOVERY.md
@.planning/phases/02-memory-store-initialization/02-RESEARCH.md
@.planning/phases/01-tooling-hygiene/01-01-SUMMARY.md
@infra/k8s/helmfile.yaml
@infra/k8s/values/cnpg-cluster/defaults.yaml
@infra/k8s/values/cnpg-cluster/local.yaml
@infra/k8s/values/cnpg-cluster/production.yaml
@infra/k8s/values/common.yaml
@infra/k8s/charts/gateway/templates/secret.yaml
@infra/k8s/charts/worker/templates/secret.yaml
@infra/k8s/values/lightrag/defaults.yaml
@infra/k8s/secrets/common.yaml
@infra/k8s/secrets/gateway/production.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align CNPG bootstrap for multi-DB + extensions</name>
  <files>infra/k8s/values/cnpg-cluster/defaults.yaml</files>
  <action>Update CNPG bootstrap SQL to create databases `mem0`, `llamaindex`, `lightrag`, and `mycontextprotocol` owned by the shared `app` role. Ensure `vector` extension is installed for `mem0` and `llamaindex`, and both `age` + `vector` are installed for `lightrag`. Avoid installing AGE globally if not needed; use per-DB SQL (post-init SQL or equivalent CNPG bootstrap hooks). Keep `shared_preload_libraries` set so AGE can load. Do not change resource sizing overrides.</action>
  <verify>Render the CNPG chart and confirm bootstrap SQL includes DB creation and extension setup for the four DBs; verify `shared_preload_libraries` includes `age,vector`.</verify>
  <done>CNPG bootstrap SQL can create all required DBs and extensions with the shared role.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize shared Postgres password in SOPS</name>
  <files>infra/k8s/secrets/common.yaml, infra/k8s/secrets/gateway/production.yaml, infra/k8s/values/common.yaml</files>
  <action>Move to a single shared Postgres password stored in the SOPS-encrypted `infra/k8s/secrets/common.yaml`. Add `mem0PostgresPassword` there (if not already), and ensure both gateway and worker charts use the same password value for `MEM0_POSTGRES_PASSWORD` and `LLAMAINDEX_POSTGRES_PASSWORD`. Remove any duplicated mem0 Postgres password from `infra/k8s/secrets/gateway/production.yaml` if present. Do not expose plaintext values in non-SOPS files.</action>
  <verify>Helmfile render for gateway/worker shows both `MEM0_POSTGRES_PASSWORD` and `LLAMAINDEX_POSTGRES_PASSWORD` sourced from the shared secret value.</verify>
  <done>Single shared password is defined once in SOPS and used for all Postgres connections.</done>
</task>

<task type="auto">
  <name>Task 3: Add CNPG credentials secret manifest</name>
  <files>infra/k8s/manifests/postgresql-credentials.yaml, infra/k8s/helmfile.yaml</files>
  <action>Create a SOPS-encrypted Kubernetes Secret manifest `postgresql-credentials` in `database` namespace with keys `username` and `password` for the shared `app` role (the keys CNPG expects). Wire this manifest into Helmfile so it is applied before the `cnpg-cluster` release (use a hook or a dedicated Helmfile `resources` entry). Keep secrets under `infra/k8s/secrets/` or `infra/k8s/manifests/` as appropriate but ensure SOPS encryption is applied.</action>
  <verify>`helmfile -l name=cnpg-cluster template` shows the CNPG cluster referencing `postgresql-credentials`, and the Secret manifest is present in the rendered output (or applied prior to sync).</verify>
  <done>CNPG cluster has the required credentials secret available at deploy time.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Helmfile render shows CNPG bootstrap SQL creates `mem0`, `llamaindex`, `lightrag`, `mycontextprotocol`
- [ ] Gateway/worker secrets render with a single shared Postgres password
</verification>

<success_criteria>

- Multi-DB bootstrap is defined with correct extensions
- Shared `app` credentials are centralized in SOPS
- CNPG credentials Secret is present and wired before cluster deploy
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Memory store initialization Summary

**[One-line summary of CNPG bootstrap + secret alignment]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
