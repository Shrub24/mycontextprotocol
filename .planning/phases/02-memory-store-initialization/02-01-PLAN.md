---
phase: 02-memory-store-initialization
plan: 01
type: execute
---

<objective>
Prepare CNPG for multi-database memory stores with correct extensions and a single shared application role.

Purpose: Ensure mem0, llamaindex, lightrag, and app-owned tables can bootstrap cleanly in Postgres without manual DB provisioning.
Output: CNPG bootstrap SQL and secret wiring aligned with multi-DB layout + shared role.
</objective>

<execution_context>
~/.config/opencode/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-store-initialization/DISCOVERY.md
@.planning/phases/02-memory-store-initialization/02-RESEARCH.md
@.planning/phases/01-tooling-hygiene/01-01-SUMMARY.md
@infra/k8s/helmfile.yaml
@infra/k8s/values/cnpg-cluster/defaults.yaml
@infra/k8s/values/cnpg-cluster/local.yaml
@infra/k8s/values/cnpg-cluster/production.yaml
@infra/k8s/values/common.yaml
@infra/k8s/charts/gateway/templates/configmap.yaml
@infra/k8s/charts/gateway/templates/deployment.yaml
@infra/k8s/charts/gateway/templates/secret.yaml
@infra/k8s/charts/worker/templates/configmap.yaml
@infra/k8s/charts/worker/templates/scaledjob.yaml
@infra/k8s/charts/worker/templates/secret.yaml
@infra/k8s/secrets/common.yaml
@infra/k8s/secrets/gateway/production.yaml
@infra/k8s/secrets/worker/production.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align CNPG bootstrap for multi-DB + extensions</name>
  <files>infra/k8s/values/cnpg-cluster/defaults.yaml</files>
  <action>Update CNPG bootstrap SQL to align with the single-namespace stack (app namespace `mycontextprotocol`) and operator-generated app credentials. Remove the explicit `bootstrap.initdb.secret` so CNPG generates the `<cluster>-app` secret. Use `postInitTemplateSQL` to install `vector` + `age` into `template1` and `postInitApplicationSQL` to create the `mem0`, `llamaindex`, and `lightrag` databases owned by `app` (no `psql` meta-commands). Keep `shared_preload_libraries` set so AGE can load. Do not change resource sizing overrides.</action>
  <verify>Render the CNPG chart and confirm `postInitTemplateSQL` installs `vector` + `age`, `postInitApplicationSQL` creates the extra DBs, and `shared_preload_libraries` includes `age,vector`.</verify>
  <done>CNPG bootstrap uses template SQL for extensions, creates all required DBs, and relies on the operator-generated `<cluster>-app` secret.</done>
</task>

<task type="auto">
  <name>Task 2: Wire internal secrets from operators + generate Dragonfly password</name>
  <files>infra/k8s/charts/gateway/templates/deployment.yaml, infra/k8s/charts/worker/templates/scaledjob.yaml, infra/k8s/charts/gateway/templates/secret.yaml, infra/k8s/charts/worker/templates/secret.yaml, infra/k8s/values/common.yaml, infra/k8s/secrets/common.yaml, infra/k8s/secrets/gateway/production.yaml, infra/k8s/secrets/worker/production.yaml</files>
  <action>Update gateway/worker env wiring so internal Postgres credentials come from the CNPG operator-generated `<cluster>-app` secret (use explicit `env:` entries with `secretKeyRef` to `password` and reuse the shared `app` role password for mem0 + llamaindex). Generate the Dragonfly password Secret (`dragonfly-password`) in the app namespace via Helm templates using `lookup` + `randAlphaNum` to preserve on upgrade. Keep external secrets (e.g., `LLAMAINDEX_OPENAI_API_KEY`) sourced from Helmfile `secrets:` values (SOPS-managed) and ensure values files contain no plaintext placeholders.</action>
  <verify>Rendered gateway/worker manifests show `secretKeyRef` entries for CNPG `<cluster>-app` and `dragonfly-password`, while external keys still come from helmfile secrets values.</verify>
  <done>Internal credentials come from CNPG + generated Dragonfly secrets, with external keys remaining user-provided via SOPS.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate namespaces for the cohesive stack</name>
  <files>infra/k8s/helmfile.yaml, infra/k8s/values/common.yaml, infra/k8s/values/cnpg-cluster/defaults.yaml, infra/k8s/values/dragonfly/defaults.yaml</files>
  <action>Update Helmfile so the app stack releases (`cnpg-cluster`, `dragonfly`, `gateway`, `worker`) deploy into the shared `mycontextprotocol` namespace while keeping operators in `cnpg-system`, `dragonfly-system`, and `keda`. Align service hostnames in `values/common.yaml` with same-namespace service discovery (e.g., `mycontextprotocol-db-rw` for Postgres and the Dragonfly service in the same namespace). Keep release names stable (`mycontextprotocol-db`, `dragonfly`).</action>
  <verify>`helmfile template` shows `cnpg-cluster`, `dragonfly`, `gateway`, and `worker` all targeting `mycontextprotocol`, while operator releases stay in their system namespaces.</verify>
  <done>All app/data plane releases run in the single app namespace with consistent naming.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Helmfile render shows CNPG bootstrap SQL creates `mem0`, `llamaindex`, `lightrag`, `mycontextprotocol`
- [ ] Gateway/worker render uses CNPG `<cluster>-app` + `dragonfly-password` and external keys provided via helmfile secrets
</verification>

<success_criteria>

- Multi-DB bootstrap is defined with correct extensions
- Shared `app` credentials are managed by the CNPG operator
- App/data plane releases run in the single `mycontextprotocol` namespace
  </success_criteria>

<output>
After completion, create `.planning/phases/02-memory-store-initialization/02-01-SUMMARY.md`:

# Phase 2 Plan 1: Memory store initialization Summary

**[One-line summary of CNPG bootstrap + secret alignment]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step

</output>
