# Local development overrides for PostgreSQL cluster
cluster:
  instances: 1
  storage:
    size: 10Gi
  resources:
    requests:
      memory: 256Mi
      cpu: 100m
    limits:
      memory: 512Mi
      cpu: 500m
