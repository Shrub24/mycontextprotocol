apiVersion: v2
name: external-secrets
description: External secrets for mycontextprotocol workloads
type: application
version: 0.1.0

dependencies:
  - name: mcp-lib
    version: 0.1.0
    repository: file://../mcp-lib
